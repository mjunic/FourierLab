# FourierLab 使用说明书

## 1. 软件用途

FourierLab 是一个本地图像频域滤波实验程序，用来演示二维傅里叶变换在图像处理中的基本过程。它适合《信息分析与处理》课程大作业展示，核心目标是把“空间域图像”和“频域频谱”之间的关系直观显示出来。

你可以完成以下实验：

- 打开一张本地图片。
- 将图片转为灰度图并计算二维 FFT。
- 查看幅度谱、相位谱和滤波器模板。
- 选择低通、高通、带通、高斯、巴特沃斯等基础滤波器。
- 调节滤波参数并重建图像。
- 查看 MSE、PSNR、频谱能量占比等指标。
- 打开 3D 频谱图，观察频谱中心和高频区域的能量分布。
- 保存处理后的结果图。

## 2. 安装和运行

### 2.1 进入项目目录

打开 PowerShell，执行：

```powershell
cd C:\Users\mjuni\FourierLab
```

### 2.2 安装依赖

推荐使用虚拟环境：

```powershell
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
```

如果不想创建虚拟环境，也可以直接安装：

```powershell
pip install -r requirements.txt
```

### 2.3 启动程序

```powershell
python main.py
```

如果提示缺少 `PySide6`，说明依赖还没有装好，请重新执行：

```powershell
pip install -r requirements.txt
```

## 3. 主界面说明

主界面分成三个实验 Tab：

- `频域滤波实验`
- `Hybrid Image 融合实验`
- `频域加密实验`

下面先说明 Tab 1 的基础频域滤波实验。

### 3.1 左侧控制区

左侧是所有操作按钮和参数。

文件区域：

- `打开图片`：选择 jpg、png、bmp 图片。
- `彩色 RGB 处理`：勾选后按彩色图处理；不勾选时默认转灰度图处理。
- `保存结果`：保存重建后的处理图。
- `重置`：回到未滤波状态。

频域滤波区域：

- `滤波器`：选择当前频域滤波方式。
- `自动低通能量`：自动调参时使用，表示希望保留多少中心累计能量。

参数区域：

- `截止半径 r`：低通、高通、巴特沃斯滤波使用。
- `带通内半径 r1`：理想带通的内边界。
- `带通外半径 r2`：理想带通的外边界。
- `高斯 sigma`：高斯低通/高通的平滑宽度。
- `巴特沃斯阶数 n`：控制巴特沃斯滤波器过渡陡峭程度。
- `参数改变后自动应用`：勾选后，拖动滑块会自动滤波并重建。

操作区域：

- `重新计算 FFT`：对当前图片重新计算频谱。
- `应用滤波并重建`：最常用按钮，会先应用滤波器，再执行 IFFT。
- `一键演示完整流程`：自动使用默认低通滤波完成 FFT、滤波和重建。
- `自动低通调参`：根据能量比例自动选择低通半径并重建。
- `仅逆变换重建`：只对当前频域结果做 IFFT。
- `查看 3D 频谱`：弹出 3D 曲面频谱图。
- `导出四宫格实验图`：导出原图、2D幅度谱、滤波器模板、重建图像，方便放入课程报告。

### 3.2 中间图像区

中间上方显示原始灰度图，下方显示重建图像。这样更适合竖向图片，例如人像、证件照、手机拍摄图片。

### 3.3 右侧频域区

右侧从上到下显示：

- 原始幅度谱 `log(|F|+1)`。
- 滤波器模板。
- 滤波后频谱。
- 相位谱。

幅度谱中心通常最亮，表示低频能量集中在中心。越靠近边缘，频率越高。

### 3.4 底部数据区

底部显示当前图像和处理结果的指标：

- 图片尺寸。
- 频谱最大值。
- 频谱平均值。
- 低频能量占比。
- 高频能量占比。
- 滤波后保留能量比例。
- 当前滤波参数。
- MSE。
- PSNR。

MSE 越小，重建图和原图越接近。PSNR 越大，重建质量通常越高。

## 4. 基本操作流程

### 4.1 打开图片

点击 `打开图片`，选择本地图片。程序会自动做三件事：

1. 读取图片。
2. 转成灰度图。
3. 计算 FFT 并显示频谱。

如果图片太大，程序会自动把最大边缩放到 1024，避免处理太慢。

如果勾选 `彩色 RGB 处理`，程序会保留彩色图，并对 R、G、B 三个通道分别执行 FFT、滤波和 IFFT。右侧频谱图为了便于观察，会显示三个通道频谱的平均结果。

### 4.2 选择滤波器

在左侧 `滤波器` 下拉框中选择一种方式。

建议课堂演示顺序：

1. 不滤波。
2. 理想低通。
3. 理想高通。
4. 理想带通。
5. 高斯低通。
6. 巴特沃斯低通。

这样从简单到复杂，比较容易讲清楚。

### 4.3 调整参数

选择滤波器后，只需要关注可用的参数。

例如：

- 选择 `理想低通`：主要调 `截止半径 r`。
- 选择 `理想高通`：主要调 `截止半径 r`。
- 选择 `理想带通`：调 `r1` 和 `r2`。
- 选择 `高斯低通`：调 `sigma`。
- 选择 `巴特沃斯低通`：调 `r` 和 `n`。

调整后点击 `应用滤波并重建`。

## 5. 各滤波器怎么理解

### 5.1 不滤波

频域模板全白，表示所有频率都保留。重建图应接近原图。

这一步适合用来证明：

```text
FFT -> IFFT 可以恢复原图
```

### 5.2 理想低通

只保留频谱中心半径 `r` 内的区域。

现象：

- r 小：图像明显模糊，只剩整体轮廓。
- r 大：图像更接近原图。

适合说明：

```text
低频表示整体亮度和大轮廓。
```

### 5.3 理想高通

去掉中心低频，保留远离中心的高频。

现象：

- 图像主体变暗。
- 边缘和细节更明显。

适合说明：

```text
高频表示边缘、纹理和细节。
```

### 5.4 理想带通

只保留 `r1` 到 `r2` 之间的频率。

注意：

- r1 要小于 r2。
- 如果 r1 和 r2 太接近，保留的信息很少，图像可能很弱。

适合说明：

```text
图像可以被拆成不同频率范围的信息。
```

### 5.5 高斯低通

高斯低通不是突然截断，而是从中心向外平滑衰减。

现象：

- 图像平滑更自然。
- 比理想低通更不容易出现明显振铃。

### 5.6 高斯高通

高斯高通是高斯低通的互补形式。

现象：

- 保留边缘。
- 过渡比理想高通更平滑。

### 5.7 巴特沃斯低通/高通

巴特沃斯滤波器介于理想滤波和高斯滤波之间。

参数 `n` 是阶数：

- n 小：过渡平缓。
- n 大：更接近理想滤波，过渡更陡。

## 6. 自动低通调参

自动低通调参会从频谱中心向外累计能量，直到达到设定比例。

例如选择 `90%`，再点击 `自动低通调参`，程序会自动找到一个半径 r，使中心圆形区域大约保留 90% 的频谱能量。

适合这样讲：

```text
大多数自然图像的能量集中在低频区域，所以保留中心一部分频谱也能重建出主要内容。
```

## 7. 3D 频谱图

点击 `查看 3D 频谱` 后，会弹出一个 Matplotlib 3D 曲面图。

横轴和纵轴表示频率位置，高度表示 `log(|F|+1)` 的大小。

观察重点：

- 中心区域通常形成高峰。
- 高频区域通常比较低。
- 滤波后，模板以外的频谱会被压低或清零。

如果图片很大，程序会自动下采样频谱再绘图，避免卡顿。

## 8. 保存结果

完成重建后，点击 `保存结果`。推荐保存为 png：

```text
fourier_result.png
```

如果没有先重建，程序会提示你先生成结果。

## 9. Hybrid Image 融合实验

进入 `Hybrid Image 融合实验` Tab。

操作步骤：

1. 点击 `导入图像 A（低频轮廓）`。
2. 点击 `导入图像 B / 文字层（高频细节）`。
3. 设置 `A 低通半径 r_low`。
4. 设置 `B 高通半径 r_high`。
5. 设置 `alpha` 和 `beta` 权重。
6. 点击 `生成 Hybrid Image`。
7. 点击 `保存融合结果`。

原理：

```text
hybrid = alpha * lowpass(A) + beta * highpass(B)
```

低频信息决定远距离视觉轮廓，高频信息决定近距离细节。你可以把一张含文字的黑白图片作为图像 B，让文字边缘作为高频细节叠加到背景图中。

## 10. 频域加密实验

进入 `频域加密实验` Tab。

操作步骤：

1. 点击 `导入图片`。
2. 设置 `seed 密钥`。
3. 设置 `相位扰动强度`。
4. 点击 `加密`。
5. 点击 `解密`。
6. 根据需要保存加密图或解密图。

原理：

图像傅里叶变换后可以分成幅度谱和相位谱。相位谱对图像空间结构非常重要。本实验使用 seed 生成随机相位扰动，破坏图像结构，再使用相同 seed 尝试恢复。

注意：这是教学型频域加密实验，不等同于真实密码学意义上的高安全等级加密算法。

## 11. 课堂展示建议

### 9.1 推荐演示 1：FFT 和 IFFT

1. 打开图片。
2. 选择 `不滤波`。
3. 点击 `应用滤波并重建`。
4. 说明重建图与原图基本一致。

结论：

```text
傅里叶变换只是换了一个表示域，信息没有丢失。
```

### 9.2 推荐演示 2：低通滤波

1. 选择 `理想低通`。
2. 把 r 调小，例如 20。
3. 点击 `应用滤波并重建`。
4. 再把 r 调大，例如 100。
5. 对比两次结果。

结论：

```text
低频越多，图像轮廓越完整；低频太少，图像会模糊。
```

### 9.3 推荐演示 3：高通滤波

1. 选择 `理想高通`。
2. 调整 r。
3. 点击 `应用滤波并重建`。

结论：

```text
高频主要包含边缘和细节。
```

### 9.4 推荐演示 4：自动低通

1. 选择能量比例 80%。
2. 点击 `自动低通调参`。
3. 再选择 95%。
4. 再点击 `自动低通调参`。

比较底部的 MSE 和 PSNR。

结论：

```text
保留能量越高，重建质量通常越好，但压缩或去噪效果会变弱。
```

## 12. 常见问题

### 12.1 程序打不开

先确认依赖是否安装：

```powershell
pip install -r requirements.txt
```

再运行：

```powershell
python main.py
```

### 12.2 为什么图片变成黑白

当前版本默认使用灰度图处理，因为灰度图只有一个二维矩阵，频谱更容易解释。如果希望保留彩色，请在打开图片前勾选 `彩色 RGB 处理`。

彩色处理的逻辑是：

```text
R 通道 -> FFT -> 滤波 -> IFFT
G 通道 -> FFT -> 滤波 -> IFFT
B 通道 -> FFT -> 滤波 -> IFFT
最后合成为彩色结果图
```

课堂讲解时建议先用灰度图说明原理，再勾选彩色处理展示完整效果。

### 12.3 为什么低通后图片变模糊

低通滤波保留的是整体轮廓，去掉了大量边缘和细节，所以图像会变模糊。

### 12.4 为什么高通后图像很暗

高通滤波去掉了低频，而低频包含大部分亮度和轮廓信息，所以重建图可能整体偏暗，只剩边缘。

### 12.5 为什么 3D 频谱图有点慢

3D 绘图比普通图片显示更耗时。程序已经对大图频谱做了下采样，课堂演示时建议使用中等大小图片。

## 13. 打包为 exe

执行：

```powershell
cd C:\Users\mjuni\FourierLab
.\build_exe.bat
```

如果遇到 PySide6 或 Matplotlib 打包资源缺失，可以尝试：

```powershell
pyinstaller -F -w -n FourierLab --collect-all PySide6 --collect-all matplotlib main.py
```

打包完成后，exe 通常在 `dist` 文件夹中。
