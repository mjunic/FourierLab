# FourierLab 协作开发与测试记录

本文档用于记录 FourierLab 项目在协作开发过程中的功能测试、运行环境和后续优化建议。  
该文件可由协作者提交，用于补充项目文档，也便于在 GitHub 中形成协作提交记录。

## 1. 项目当前功能概述

FourierLab 是一个面向《信息分析与处理》课程的二维傅里叶图像实验平台，当前主要包含三个核心模块：

1. **基础傅里叶实验**
   - 图像导入
   - 二维 FFT 变换
   - 幅度谱与相位谱显示
   - 低通、高通、带通、带阻等频域滤波
   - 傅里叶逆变换重建图像
   - 频谱指标与操作日志显示

2. **频域数字水印**
   - 输入文字水印
   - 将文字转为二进制比特流
   - 在图像傅里叶频谱的中频区域嵌入水印
   - 保存含水印图像与参数 JSON
   - 使用相同参数提取水印内容

3. **频域加密解密**
   - 通过 FFT 得到图像频谱
   - 使用随机相位扰动实现教学型频域加密
   - 保存加密预览图 PNG
   - 保存可解密加密包 NPZ
   - 使用正确密钥和加密包恢复图像

## 2. 运行环境建议

推荐环境：

```bash
Python 3.10 或 3.11
Windows 10 / Windows 11
```

安装依赖：

```bash
pip install -r requirements.txt
```

运行软件：

```bash
python main.py
```

## 3. 基础功能测试流程

### 3.1 基础傅里叶实验

测试步骤：

1. 打开软件。
2. 进入“基础傅里叶实验”页面。
3. 点击“导入图片”。
4. 执行 FFT。
5. 观察幅度谱和相位谱。
6. 选择低通滤波或高通滤波。
7. 执行 IFFT 重建。
8. 保存处理结果。

预期现象：

- 低通滤波后图像整体更加平滑；
- 高通滤波后图像边缘和纹理更突出；
- 幅度谱中心区域通常更亮，说明图像主要能量集中在低频区域。

### 3.2 频域数字水印实验

测试步骤：

1. 导入一张测试图像。
2. 输入水印文字，例如：`FourierLab2026`。
3. 设置 seed、delta、r1、r2 等参数。
4. 点击“嵌入水印”。
5. 保存含水印图像 PNG。
6. 保存水印参数 JSON。
7. 重新导入含水印图像。
8. 读取 JSON 参数。
9. 点击“提取水印”。

预期现象：

- 含水印图像与原图视觉差异较小；
- 使用相同参数可以提取出原始水印文字；
- 如果图像被 JPEG 压缩或尺寸发生变化，水印可能无法稳定提取。

### 3.3 频域加密解密实验

测试步骤：

1. 导入测试图像。
2. 设置 seed 和相位扰动强度。
3. 点击“加密”。
4. 保存加密预览图 PNG。
5. 保存可解密加密包 NPZ。
6. 导入 NPZ 加密包并解密。

预期现象：

- 加密预览图呈现明显扰乱效果；
- 使用 NPZ 加密包和正确参数可以恢复较接近原图的解密图；
- 单独使用 PNG/JPG 加密图不能保证完整解密，因为普通图像文件无法保存完整复数频谱信息。

## 4. 协作开发注意事项

1. 提交代码前建议先运行：

```bash
python main.py
```

确认软件可以正常打开。

2. 不建议提交以下文件：

```text
__pycache__/
*.pyc
build/
dist/
*.spec
.venv/
venv/
```

3. 如果新增依赖，请同步更新：

```text
requirements.txt
```

4. 如果修改了运行方式或功能流程，请同步更新：

```text
README.md
使用说明与原理.md
```

## 5. 后续优化建议

1. 优化 UI 布局，使按钮更加符合实验流程。
2. 增强水印嵌入与提取的鲁棒性。
3. 增加一键导出实验截图功能。
4. 增加更多示例图片，方便课程展示。
5. 完善 exe 打包流程，形成课堂展示版文件夹。

## 6. 建议提交信息

如果本文件由协作者提交，可以使用如下 commit message：

```bash
git add docs/TEAM_TEST_NOTES.md
git commit -m "Add team testing notes"
git push
```
