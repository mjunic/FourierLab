# FourierLab 使用说明与原理

## 1. 项目简介

FourierLab —— 二维傅里叶图像实验平台，是面向《信息分析与处理》课程的学习型实验软件。当前版本聚焦三个核心 Lab：

1. 基础傅里叶实验
2. 频域数字水印
3. 频域加密解密

软件目标是帮助学生理解图像作为二维离散信号的表示方式，观察二维 FFT 后的幅度谱和相位谱，并通过频域滤波、水印嵌入和随机相位掩膜加密理解傅里叶变换在图像处理中的作用。

## 2. 安装与运行

```powershell
cd C:\Users\mjuni\FourierLab
pip install -r requirements.txt
python main.py
```

打包 exe：

```powershell
.\build_exe.bat
```

## 3. Lab 1：基础傅里叶实验

基础傅里叶实验包含：

- 导入图片
- 灰度 / 彩色 RGB 处理
- 二维 FFT
- 幅度谱显示
- 相位谱显示
- 滤波器模板显示
- 低通、高通、带通、带阻、高斯、巴特沃斯滤波
- IFFT 重建
- MSE、PSNR、低频能量占比、高频能量占比、保留能量比例
- 3D 频谱
- 实验图导出
- 操作日志与原理说明

二维 DFT：

```text
F(u,v)=ΣΣ f(x,y)e^{-j2π(ux/M+vy/N)}
```

二维 IDFT：

```text
f(x,y)=1/(MN)ΣΣ F(u,v)e^{j2π(ux/M+vy/N)}
```

频域滤波：

```text
G(u,v)=F(u,v)H(u,v)
```

幅度谱：

```text
A(u,v)=|F(u,v)|
```

相位谱：

```text
P(u,v)=arg(F(u,v))
```

频谱显示：

```text
S(u,v)=log(1+A(u,v))
```

低通滤波保留低频，图像更平滑；高通滤波保留高频，突出边缘；带通保留某一频段；带阻去除某一频段。

## 4. Lab 2：频域数字水印

频域数字水印实验将文字水印嵌入图像傅里叶频谱的中频区域。选择中频区域的原因是：

- 低频区域影响图像主体，修改后容易被察觉。
- 高频区域容易被压缩和去噪破坏。
- 中频区域兼顾隐蔽性与稳定性。

水印嵌入流程：

1. 将水印文字用 UTF-8 编码为字节。
2. 将字节转换为 bit 流。
3. 对图像执行 FFT。
4. 使用 seed 在中频环带 r1~r2 中伪随机选择频率点。
5. 同时修改共轭对称位置，使 IFFT 后图像保持接近实数。
6. 使用简化 QIM 修改频谱幅度。
7. IFFT 得到含水印图像。

QIM 简化思想：

```text
bit=0: 将幅度调整到量化区间的一类中心
bit=1: 将幅度调整到另一类中心
```

提取时使用相同 seed、Δ、r1、r2 和 bit_length 找到相同频率点，并根据幅度所在量化区间恢复 bit。

参数说明：

- seed：控制伪随机嵌入位置。
- Δ：嵌入强度。越大越容易提取，但图像失真也越明显。
- r1：中频内半径。
- r2：中频外半径。
- bit_length：提取时需要的水印 bit 数。

注意：普通 PNG/JPG 会造成量化或压缩误差，可能影响提取。软件同一次运行中会保留嵌入频谱，保证教学演示可稳定提取。

推荐的保存后提取流程：

1. 嵌入水印。
2. 点击“水印自检”，确认算法本身能提取。
3. 保存含水印图像，推荐 PNG。
4. 保存参数 JSON。
5. 重新导入含水印 PNG。
6. 读取 JSON 参数。
7. 提取水印。

JPG 压缩会改变像素和频谱幅度，可能破坏水印。PNG 更适合本实验。由本软件保存的 PNG 会额外写入教学辅助元数据，便于说明“频域嵌入”和“保存格式”之间的关系；真正的频域嵌入仍然发生在图像频谱中。

## 5. Lab 3：频域加密解密

频域加密解密实验使用随机相位掩膜扰乱图像相位谱。该功能是教学型实验，不是严格密码学加密。

加密流程：

```text
F(u,v)=FFT2(f(x,y))
A(u,v)=|F(u,v)|
P(u,v)=arg(F(u,v))
R(u,v)=RandomPhase(seed)
P_enc(u,v)=P(u,v)+strength·R(u,v)
F_enc(u,v)=A(u,v)·exp(j·P_enc(u,v))
encrypted=IFFT2(F_enc)
```

解密流程：

```text
P_dec(u,v)=P_enc(u,v)-strength·R(u,v)
F_dec(u,v)=A_enc(u,v)·exp(j·P_dec(u,v))
decoded=IFFT2(F_dec)
```

软件支持保存 `.npz` 加密包。加密包中保存复数频谱和参数：

- encrypted_spectrum
- seed
- strength
- image_shape
- color_mode

普通 png/jpg 只能保存空间域像素，不能保存完整复数频谱和相位扰动状态，因此加密预览图不能保证单独用于完整解密。完整解密应使用 `.npz` 加密包。

界面中两种保存方式含义不同：

- 保存加密预览图 PNG：只用于观察密文外观，不保证可逆解密。
- 保存可解密加密包 NPZ：保存复数频谱和参数，用于完整解密演示。

## 6. 数据指标说明

MSE：均方误差，越小表示两张图越接近。

PSNR：峰值信噪比，越高表示重建质量通常越好。

低频能量占比：频谱中心低频能量占总能量的比例。

高频能量占比：边缘、纹理和噪声等高频成分的能量比例。

## 7. 课程展示建议

推荐演示顺序：

1. Lab 1：导入图片，显示幅度谱和相位谱。
2. Lab 1：低通滤波，说明低频对应整体轮廓。
3. Lab 1：高通滤波，说明高频对应边缘细节。
4. Lab 2：输入 `FourierLab2026`，嵌入水印并提取。
5. Lab 2：说明中频水印和 QIM 思想。
6. Lab 3：使用 seed=1236、strength=1.0 加密。
7. Lab 3：同一次运行解密，并说明 `.npz` 加密包的重要性。

## 8. 参考思想说明

本软件参考傅里叶变换图像处理、频域水印、随机相位掩膜和图像加密等思想，但实现为课程教学简化版本，重点是可视化理解和实验展示，不追求商用安全等级。
